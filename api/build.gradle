plugins {
    id 'maven-publish'
}

tasks.register('sourceJar', Jar) {
    from sourceSets.main.allJava
}

publishing {
    def artifact = "MinigameAPI"
    def group = 'com.github.xFedeT'

    publications {
        maven(MavenPublication) {
            groupId = group
            artifactId = artifact
            version

            from components.java
        }
    }
}